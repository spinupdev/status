{
  "name": "zeish-status-page",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/healthcheck.ts",
    "build": "node scripts/build.ts"
  }
}
